{
  "name": "@carl/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo \"@carl/desktop is built with: pnpm --filter @carl/desktop bundle\"",
    "bundle": "tauri build",
    "dev": "tauri dev",
    "check": "cd src-tauri && cargo check",
    "typecheck": "echo \"@carl/desktop has no TypeScript of its own yet\"",
    "tauri": "tauri"
  },
  "dependencies": {
    "@carl/sync": "workspace:*",
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-sql": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
