/**
 * `@carl/ui` — Carl's shared component library.
 *
 * Presentation only. Components take data and emit events; they never fetch, never compute a
 * total, and never decide whether an action is permitted.
 */

export * from './cn.js';
